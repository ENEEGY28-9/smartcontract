🎯 WALLET TESTING GUIDE - COPY-PASTE CODES
=========================================

🧪 COPY-PASTE THESE INTO BROWSER CONSOLE (F12):

📋 1. CHECK CURRENT WALLET:
```javascript
console.log('Current wallet:', window.solana?.publicKey?.toString());
console.log('Connected:', window.solana?.isConnected);
console.log('Network: Solana Devnet');
```

📋 2. TEST CUSTOM PUBLIC KEY:
```javascript
async function testKey(key) {
  try {
    const connection = new solanaWeb3.Connection('https://api.devnet.solana.com');
    const publicKey = new solanaWeb3.PublicKey(key);
    const balance = await connection.getBalance(publicKey);
    console.log(`✅ ${key.slice(0,8)}...: ${balance / solanaWeb3.LAMPORTS_PER_SOL} SOL`);
  } catch(e) {
    console.error(`❌ ${key.slice(0,8)}...: Invalid`);
  }
}

testKey('57arMrLe8LHfzn7c0yUu6KGhxLQ6nfP87mHTHpM2SGB'); // Your wallet
testKey('EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v'); // System
```

📋 3. SWITCH WALLET TEST:
```javascript
console.log('🔄 Switch test:');
console.log('Previous:', '57arMrLe8LHfzn7c0yUu6KGhxLQ6nfP87mHTHpM2SGB');
console.log('Current: ', window.solana?.publicKey?.toString());
console.log('Changed:', window.solana?.publicKey?.toString() !== '57arMrLe8LHfzn7c0yUu6KGhxLQ6nfP87mHTHpM2SGB');
```

📋 4. COMPREHENSIVE WALLET INFO:
```javascript
console.log('💼 WALLET INFO:');
console.log('Address:', window.solana?.publicKey?.toString());
console.log('Connected:', window.solana?.isConnected);
console.log('Network:', window.solana?.isPhantom ? 'Phantom' : 'Other');
console.log('Methods:', Object.keys(window.solana || {}).join(', '));
```

🌐 TESTING URLs:
- Main Test: http://localhost:5176/wallet-test
- Custom Key: http://localhost:5176/wallet-custom-key-test.html
- Connection: http://localhost:5176/test-phantom-connection.html

🎮 READY FOR GAME DEVELOPMENT:
✅ Test with real wallet data
✅ Switch between multiple accounts
✅ Check balances from blockchain
✅ Validate wallet addresses
✅ Multi-wallet scenarios

🚀 Your wallet integration is PROFESSIONAL GRADE! 🎯✨

Happy coding! 🎮

